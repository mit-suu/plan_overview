# Ghi chú UX khi đi như user thật (S-3.6 → S-9)

- [S-3.6] Reload trang khi đang ở gate ⇒ gate mất, step về `pending`, phải chạy lại (tốn credit). Panel SRS hiện "stale" không giải thích.
- [S-3.6] Yêu cầu qua chat "thêm UC nhắc lịch 24h + UC đánh dấu no-show" ⇒ AI trả lời "Tôi sẽ bổ sung UC18, UC19" nhưng Spine vẫn 17 UC — chat elicit chỉ ghi addendum, không sửa thật. User tưởng đã xong. Không có chỉ báo "đã ghi / chưa ghi".
- [S-3.6] Sau khi yêu cầu, AI lại hỏi về tách function (việc của S-4/S-5) ngay trong step Sơ đồ use case.
- [S-3.6] **Chat input** có placeholder "…hoặc lệnh yêu cầu chỉnh sửa" nhưng không sửa được; **công cụ sửa thật** nằm ở icon bút chì rail phải. Hai chỗ cùng hứa "sửa" ⇒ user không biết dùng cái nào.
- [S-3.6] Công cụ "Sửa tài liệu có xem trước": 3 lần thêm use case đều thất bại: `duplicate_id: A03`, `path_not_resolved: use_cases[id=UC-REMIND]`, `path_not_resolved: use_cases[id=UC18]` (lệnh rõ ràng nhất). Lỗi hiện nguyên mã kỹ thuật; vẫn có nút "Xác nhận" cạnh "Không có thay đổi nào". Lệnh sửa bị xoá khi đóng/mở panel. Nguyên nhân: skill `apply-change-op` chỉ dạy `set` phần tử có sẵn, không dạy `add` + cấp id mới, projection không có danh sách id.
- [Chung] Access token hết hạn sau 15 phút (`exp - iat = 900`) ⇒ fetch trả `data: null`; FE không tự refresh ở một số lời gọi — reload mới được.
- [S-3.6] Chat tự do (elicit) hỏi vòng vo không dừng: "chốt 5 actor?" → "chốt 7 actor?" → rồi tự bịa "Bước tiếp theo là S-3.8 – NFR" (không có step này; NFR là S-6). Chat nói "đã chốt 7 actor" nhưng Spine vẫn 6 actor. User không phân biệt được chat nào ghi thật, chat nào chỉ nói.
- [S-4.1] **Crash**: sau khi trả lời 4 câu (câu 1 có bổ sung "cần thêm use case nhắc lịch + no-show"), step lỗi `Lỗi NOT_IMPLEMENTED: values is not iterable`. Nghi: model `add` phần tử mới (UC/screen/function) thiếu mảng (`includes`/`extends`/`function_ids`/`flow_to`/`business_rule_ids`), `reference-fields.ts:160` `many()` lặp `undefined` trước khi schema/invariant bắt ⇒ TypeError bị quy thành 501 thay vì lỗi validate để retry. User chỉ thấy mã lỗi kỹ thuật + nút "Đóng".
- [S-4.1] Sau lỗi trên, bấm "Chạy bước này" lại ⇒ `SPINE_VERSION_CONFLICT: The document just changed in another session. Please reload` — không có session nào khác; FE giữ `base_version` cũ sau lỗi. Thông báo tiếng Anh, đổ lỗi sai chỗ, buộc user reload.
- [S-4.1] Lần 2 hỏi lại một phần đã trả lời (tách màn). Gate 31 ops chỉ hiện con số — để biết 7 màn là gì phải tự mở Spine/ghép lại tài liệu. F06 "Automated Notifications" được tạo nhưng **vẫn không có UC** nhắc lịch/no-show (yêu cầu lần 3 vẫn không vào).
- [S-4.2] Luồng màn nháp đầu: sau Log In chỉ tới My Appointments — màn Lễ tân/Bác sĩ/Quản trị không vào được. Request revision (1 lần) sửa được điều hướng theo vai trò ✅, nhưng bỏ sót ý thứ 2 (S02 → S04). Muốn xem sơ đồ phải ghép lại tài liệu — gate không hiện ảnh sơ đồ vừa vẽ.
- [S-4.3] Ma trận quyền nháp tốt; thiếu create/delete cho Admin trên trang CRUD — Request revision sửa đúng (3 ops) ✅.
- [S-4.4] Tốt: 4 function nền có trigger cụ thể (nhắc SMS, nhả giữ chỗ 15', no-show, webhook VNPay — tự thêm webhook hợp lý). Lệch: id `FN01..FN04` còn function màn là `FN001..` (2 kiểu id). Vẫn không có UC tương ứng ⇒ function mồ côi UC.
- [S-4.5] Câu hỏi ERD gợi ý thực thể chung chung không có trong phạm vi (Invoice, Room, Service) — user phải gõ lại danh sách đúng. Kết quả ERD hợp lý (11 thực thể). Thiếu: Doctor ↔ Staff Account; schema `entities` không có thuộc tính ⇒ ERD chỉ có hộp tên, BA/dev không thấy field chính.
- [S-5] Tổng số step nhảy từ 51 lên **91** (S-5 = 8 nhóm × 5). S-5.1 "Hàng đợi màn" là gate trống không có gì để duyệt.
- [S-5.2@S02] Câu hỏi tốt, cụ thể, gợi ý sát (trigger, quy tắc hiển thị, thông báo). ✅
- [S-5.3@S02] "Bố cục màn" không hỏi gì, gate không ghi op; D05 "screen_layout" chỉ là bảng 2 cột Function|Description (salt) — không phải wireframe (không có danh sách, nút, trường). BA kỳ vọng ít nhất khung layout.
- [S-5.2@S03] Gợi ý đáp án mâu thuẫn quy tắc đã chốt ở S-2.4 ("tối đa 3 lịch/ngày" vs "tối đa 1 lịch đang chờ"; "đặt trước 30 phút" không có trong brief). Model không đọc business_rules đã có ⇒ user phải nhớ và sửa tay; user vội chọn gợi ý đầu sẽ tạo mâu thuẫn.
- [S-5.4@S03] Hỏi lại gần y hệt S-5.2 (giữ slot, trùng giờ, message) và S-4.4 (giữ chỗ 15') — lặp câu hỏi giữa các step liền nhau.
- [S-5.2@S04] Lần 2: gợi ý cọc 30%/50%/100.000đ trong khi đã chốt 50.000đ cố định từ B-0.1 — gợi ý không bám Spine.
- [S-5.4@S04] Màn S04 chỉ có 1 function "Pay Booking Deposit"; đổi lịch/huỷ lịch (UC05/UC06) không có function nào. Trả lời câu hỏi + Request revision "thêm 2 function" ⇒ **0 ops**, không báo lý do. Mẫu chung: **phát hiện thiếu ở step sau thì không có đường bổ sung** (UC ở S-3.3, function ở S-4.1 đã accept; edit tool không thêm được phần tử). Không có nút "quay lại step X".
- [S-5] `messages` vẫn = 0 dù S-5.2/S-5.4 hỏi rất kỹ nội dung message — user không biết message đã chốt nằm đâu (tới S-7.3 mới ghi?).
- [S-5.4@S04 → S05] **Hỏng dữ liệu nghiêm trọng**: Request revision "thêm 2 function Reschedule/Cancel cho S04" hiển thị 0 ops nhưng thực tế **ghi đè FN005 "Check In Patient" (màn Lễ tân) thành "Reschedule Appointment"** (vẫn ở S05, trigger của Patient). Chức năng check-in biến mất, không cảnh báo. Cùng lớp lỗi cấp id với `duplicate_id A03`: model tự đánh id tiếp theo trùng id đã có ⇒ `set` đè thay vì `add`.
- [Edit tool] Sửa lại FN005: lần 1 `SPINE_VERSION_CONFLICT` (panel sửa giữ base_version cũ sau khi runner ghi — phải reload); lần 2 `op_not_allowed … validations` (skill sửa không biết add/remove từng phần tử mảng có id); lần 3 bỏ validations thì **preview diff rất tốt** (bảng Before/After + phạm vi ảnh hưởng + "đang bị tham chiếu bởi") ✅ → Xác nhận OK nhưng không có thông báo "đã áp dụng". Xoá validations sai: vẫn `op_not_allowed`.
- [S-5.2@S05] Sau reload (bắt buộc vì conflict), bấm Chạy ⇒ `STEP_NOT_RUNNABLE: This step isn't ready to run or review yet` (tiếng Anh, không nói lý do). Network: "Step đang được xử lý ở một request khác" — khoá in-process `runningSteps` của lượt chạy cũ (SSE đã đóng) **kẹt ~15–20 phút** tới khi hết `ANSWER_WAIT_TIMEOUT_MS`. User thật không có cách gỡ ngoài chờ; không có nút huỷ lượt chạy.
- [S-5.4@S05] Hỏi lại message đã chốt ở S-5.2 cùng màn (lần này đòi tiếng Việt, trong khi SRS cố định tiếng Anh). Kết quả S-5.3@S05 hiển thị lại nguyên đoạn chat cũ của S04 ('Về việc thêm function Đổi lịch và Huỷ lịch… FN005/FN006') — context chat lẫn giữa các màn.
- [S-5.2@S06] Điểm cộng: model tự phát hiện FN006 mang luồng 'huỷ lịch' nhầm và hỏi lại ✅. SPINE_VERSION_CONFLICT lặp lại khi Accept S-5.5@S05 ⇒ phải reload lần thứ 4.
- [S-5.2@nonscreen] Câu hỏi lộ khái niệm nội bộ ('screen ảo @loop', 'ghi vào permissions cho screen @loop') — BA không hiểu. Hàng đợi nhảy S06 → nonscreen, S01/S07 chưa làm.
- [S-5 → S-6] **Bỏ sót màn**: sau S06 hàng đợi nhảy sang `nonscreen` rồi thẳng S-6.1. `screen_queue` vẫn = [S02,S03,S04,S05,S06,S01,S07] nhưng `screen_cursor=null`; S01 (Auth) và S07 (Admin Console) còn `placeholder`, 10 step S-5.x@S01/@S07 `pending`. Panel Tiến độ liệt kê các step này nhưng **disabled** — không có cách quay lại. Tiến độ vẫn báo "69% hoàn thành" phần S-5 mà không cảnh báo.
- [S-6.2→S-7.1] 5 step liền không hỏi gì, tự suy từ Spine — trải nghiệm tốt nhất cả quy trình. S-7.2 hỏi uptime lần thứ 3 (S-1.4, S-6.1, S-7.2).
- [S-8.1] Hỏi định dạng bảng thuật ngữ — template FPT đã quyết định, câu hỏi thừa.
- [S-8.2] Tài liệu ghép: 3.2.1 Authenticate Account và 3.6.1 Manage Branch Records hiện "Draft · Chưa hoàn thiện — nội dung sẽ có khi step sở hữu section chạy" nhưng step đó không chạy được (bị bỏ qua ở S-5). Heading feature cha vẫn gắn "Accepted"; 3.7 vừa "Accepted" vừa "Chưa hoàn thiện". Không có cờ đỏ nào bắt màn placeholder. 2 cờ đỏ `diagram_stale` (D06, D08) — user không biết cách render lại (không có nút). Panel SRS có nhãn "Cũ" cho section, không giải thích.
- [S-8.3/S-8.4] Gate không có nội dung: S-8.4 'Rà nhất quán' không nói đã rà gì, tìm thấy gì, sửa gì (0 ops) — chỉ còn dòng đếm cờ. Chat vẫn hiển thị lịch sử từ S-5, không phân đoạn theo step.
- [S-9.1] **27 giả định chưa xác nhận lộ ra cùng lúc** (cờ đỏ 2 → 35). Chưa gate nào trước đó cho xem/xác nhận giả định. Vài giả định **trái với câu trả lời của user**: AS28 uptime 99.5% (user nói 99% ba lần), AS34 500 người dùng đồng thời, AS36 "chỉ in-app, không email" (email xác nhận là yêu cầu gốc). AS08/AS09 cho thấy model **tự quyết để trống màn Auth và Admin** — không hề báo user.
- [Verification panel] Mỗi cờ chỉ có "Waive" và "→ step"; **không có nút Xác nhận/Bác bỏ giả định**. Cách duy nhất: Request revision ở S-9.1 gõ tay danh sách ID (AS07, AS10…) — kết quả tốt (35 → 10 cờ đỏ, AS28/34/36 sửa đúng) ✅, nhưng NFR N08 vẫn ghi ≥ 99.5% ⇒ giả định sửa mà dữ liệu sinh ra từ nó không sửa theo.
- [S-9.1] Phản đối "để trống S01/S07" biến thành 2 giả định mới AS37/AS38 (unconfirmed, đỏ) mà không có đường thực hiện.
- [Reconcile] "Hoà giải một lượt" với 6 section `stale_at_baseline` ⇒ "Không có thay đổi nào", nút Xác nhận disabled ⇒ không gỡ được cờ đỏ stale.
- [S-9.4] MoSCoW không hỏi user, tự gán: FN01 nhắc lịch SMS = COULD (cơ chế chính của mục tiêu giảm 30% no-show!), FN004 thanh toán cọc = SHOULD, FN03 no-show = SHOULD. Gate không hiển thị bảng ưu tiên — phải mở Spine mới thấy. S-9.2/S-9.3 gate cũng không hiện kết quả soát/đối chiếu.
